{
  "name": "reason-tree-sitter",
  "version": "1.0.1000",
  "description": "Reason bindings for tree-sitter",
  "license": "MIT",
  "scripts": {
    "format": "esy dune build @fmt --auto-promote"
  },
  "esy": {
    "build": ["refmterr dune build -p onigurama"],
    "buildsInSource": "_build",
    "install": [
        "esy-installer onigurama.install"
    ]
  },
  "resolutions": {
    "@opam/biniou": "1.2.0",
    "@opam/easy-format": "1.3.1"
  },
  "dependencies": {
    "@opam/dune": "^1.6.0",
    "@esy-ocaml/reason": "^3.4.0",
    "@reason-native/rely": "^1.3.1",
    "refmterr": "*",
    "ocaml": ">=4.6.0",
    "esy-onigurama": "^5.9.1000",
    "reperf": "^1.3.0"
  },
  "devDependencies": {
    "ocaml": ">=4.6.0"
  }
}
